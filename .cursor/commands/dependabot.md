# Dependabot triage

Follow @docs/SECURITY_TRIAGE.md and @KNOWLEDGE_BASE.md.
List open Dependabot alerts and PRs via `gh`; prioritize Critical/High.
Merge safe `cargo` / `github-actions` bumps; run `feature-gate.ps1 -Stack lightroom-rust -Ci` after lockfile changes.
Document temporary deferrals in @DECISION_LOG.md.

Begin now.
