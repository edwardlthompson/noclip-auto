# Parallel agent scope map

Read @docs/PARALLEL_AGENT_SCOPES.md and BUILD_PLAN Parallel table.
Run `.\scripts\check-parallel-scope.ps1`; abort dispatch on overlap.
Assign one branch per agent: `feature/agent-<task-slug>`. Shared pipeline/schema stays Sequential-only.

Begin now.
