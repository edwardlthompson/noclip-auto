# Stack verification (lightroom+rust)

Active stack: **lightroom + rust** only. No web/python/android examples.

1. Read @AGENT_MEMORY.md and @docs/BOOTSTRAP_TEMPLATE_MAP.md.
2. Confirm production paths: `NoClipAuto.lrdevplugin/`, `noclip-analyze/` (not `examples/` stubs).
3. Confirm `examples/golden-path/README.md` documents Golden Path without code duplication.
4. Confirm absent stacks are not referenced in active BUILD_PLAN rows.
5. Run `.\scripts\validate-bootstrap.ps1 -Quick`.

Begin now.
