# Session state restore

Read `.cursor-session-state.json` if present (schema: `.cursor-session-state.example.json`).
Restore repo mode, stack (`lightroom-rust`), sprint, and sequential step. Pick Cursor mode via @docs/CURSOR_MODES.md.
Delete the session state file after restore.

Begin now.
